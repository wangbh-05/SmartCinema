# SmartCinema 智能影院选座系统课程大作业报告

> 课程：《Web 前端技术实训》2026 夏  
> 选题：SmartCinema 智能影院选座系统  
> 小组：豆浆糖油饼  
> 提交分支：`experiment/final-sprint`  
> 核验基线：`3e06261`（2026-08-14）

| 姓名 | 学号 | 分工 | 联系邮箱 |
|---|---:|---|---|
| 王博翰 | 2024012510 | 组长；初始框架、开发流程设计、答辩材料 | wangbh24@mails.tsinghua.edu.cn |
| 潘宇治 | 2024012509 | 第三阶段后续开发、答辩材料 | panyz24@mails.tsinghua.edu.cn |
| 赵常景行 | 2024012513 | 第四阶段后续开发、答辩材料 | zcjx24@mails.tsinghua.edu.cn |
| 杨世威 | 2024012512 | 第二阶段测试与优化 | 1956559268@qq.com |

---

## 摘要

SmartCinema 是一套面向消费者的智能影院选座与本地购票演示系统。项目以“别让用户思考”为核心原则，把传统影院购票中分散的场次查询、票种选择、座位判断、价格计算、锁座、登录和订单操作组织为一条连续交易漏斗，并通过智能连座推荐、自然语言座位顾问、Canvas 热度地图和四维体验评分降低用户的决策成本。

项目使用原生 HTML5、CSS3 与 JavaScript ES Modules 实现，不依赖前端框架或第三方图表库。座位图和连续热力场均由 Canvas 2D 绘制；业务代码采用 UI、应用、领域、基础设施分层；用户、库存、锁座、订单和辅助偏好统一保存在经过版本与引用完整性校验的 LocalStorage 状态中。系统同时提供管理员运维入口、响应式触摸交互以及键盘和屏幕阅读器可访问能力。

在实现过程中，我们没有把作业要求简单翻译为控件清单，而是把真实影院业务中的隐私最小化、库存竞争、锁座时效、票价政策、退票政策、无障碍席位和跨设备交互纳入设计。部分功能形态因此与要求示例不同，但其能力目标得到保留，并形成了更接近真实售票产品的流程。

**关键词：** Canvas；智能选座；可解释推荐；LocalStorage；响应式设计；无障碍；AI 协同开发

---

## 1. 项目背景与目标

影院选座表面上是“在座位图上点几个位置”，实际包含多重决策：用户要在观影效果、同行关系、价格、剩余库存、儿童或长者需求、进出便利性之间作出权衡。影厅越大、同行人数越多，选择成本越高。

我们将项目目标归纳为四点：

1. 让用户快速找到可购买且适合自己的座位，而不是只展示库存；
2. 用可解释的推荐理由和体验指标降低选择的不确定感；
3. 让选座、锁座、下单和退票遵守一致的业务状态与价格规则；
4. 在 PC、平板和手机上提供连贯操作，并照顾键盘、读屏和色觉差异用户。

项目最终形成两个入口：

- `index.html`：消费者购票入口，覆盖完整购票闭环；
- `internal.html`：受管理员权限保护的放映运维入口，设置 `noindex`，普通消费者导航不暴露该入口。

## 2. 用户分析与产品定位

### 2.1 目标用户

| 用户类型 | 典型需求 | SmartCinema 的应对方式 |
|---|---|---|
| 大学生与朋友 | 预算敏感、多人连座、希望快速完成 | 学生票、价格平衡评分、朋友/团体推荐、最多 5 组候选 |
| 情侣 | 中后排、居中、连续双座 | 情侣同行模式，对同排、中心和中后排进行优先排序 |
| 家庭 | 连座并照顾儿童或长者 | 儿童避开前三排、长者避开后三排；不足时明确说明降级 |
| 团体观众 | 5–20 人、尽量不分散 | 优先同排连座；库存不足时仅降级为紧凑的相邻两排 |
| 老年人与残障用户 | 字体清晰、操作简单、无台阶通行 | 大字体、高对比、色觉友好、减少动态、轮椅与陪同席联动 |
| 临时访客 | 尚未决定购买，不愿先注册 | 可匿名浏览、选座和短时锁座；确认订单前再登录 |

### 2.2 核心产品原则

- **先给结果，再允许细调。** 用户可以一键获得推荐，也可以继续换一组或手动修改。
- **规则优先于分数。** 已售、库存冲突、票座数量、无障碍与孤座规则先校验，体验分不能覆盖业务违规。
- **解释每次取舍。** 推荐会说明座位、偏好满足情况、年龄建议、跨排或跨过道情况及附加费。
- **渐进式要求身份。** 浏览与试选无需登录，产生订单时才进入身份环节。
- **最少采集信息。** 推荐只使用人数、票种、同行方式和座位偏好，不保存无必要的同行成员姓名与精确年龄。

## 3. 需求理解与总体方案

### 3.1 核心流程

系统顶部将用户任务归纳为三个阶段：

```text
① 选择场次
   影片 → 影院 → 日期 → 放映时间
            ↓
② 票数与座位
   票种/人数 → 一键推荐、AI 顾问或手动选座 → 体验与价格确认
            ↓
③ 确认订单
   创建 10 分钟锁座 → 登录/注册 → 确认订单 → 我的订单/退票
```

阶段内允许用户调整选项，但不会要求反复跨页面往返。移动端底部固定摘要持续显示票数、已选数量、价格和下一步状态，桌面端则使用粘性订单摘要。

### 3.2 功能范围

系统实现了以下主要能力：

- 登录、注册、会员角色与管理员权限；
- 7 部影片、3 家虚构影院、6 个影厅、连续 3 个营业日和 108 个场次；
- 100、200、300 座三种规模的 10 排弧形 Canvas 影厅；
- 成人、儿童、学生、银发四类票种和统一政策报价；
- 一键连座推荐、最多 5 组替换方案和同日相邻场次建议；
- 自然语言 AI 座位顾问及本地规则回退；
- 单击多选、鼠标框选、移动端滑动连选、平移和双指缩放；
- Canvas 连续热力场、一周七日与综合热度切换；
- 视野、距离、周边空位、价格平衡四维体验评分；
- 10 分钟锁座、订单确认、订单查询、整单退票和库存恢复；
- 大字体、高对比、色觉友好、减少动态和读屏语义层；
- 管理员指标看板、锁座清理/释放、诊断导出和备份导入。

### 3.3 功能完成清单（按评分项）

为便于验收，下表将课程要求与最终实现逐项对应。“完整实现”表示实现形态与要求一致；“场景化实现”表示能力目标已经覆盖，但团队结合真实影院业务、隐私或跨设备操作调整了具体交互；“额外实现”表示超出基础要求。

| 类别 | 课程功能点 | 完成情况 | 最终实现与验收入口 |
|---|---|---|---|
| 基本功能 | 用户登录、注册，注册用户获得会员资格 | **完整实现** | 首页“登录/注册”；注册后获得 `member` 角色并可确认订单、查看订单 |
| 基本功能 | 管理员登录并进入后台 | **完整实现** | 使用管理员账号登录后访问 `internal.html`；访客和普通会员会被拒绝 |
| 基本功能 | 小/中/大厅分别为 100/200/300 座、每厅 10 排 | **完整实现** | 2/3 号厅为 100 座，5/6 号激光厅为 200 座，IMAX/巨幕厅为 300 座 |
| 基本功能 | Canvas 弧形座位图、排号和座号 | **完整实现** | 原生 Canvas 2D 绘制，行号置于每排两侧、座号绘制在座位内；完整标签同步到读屏层 |
| 基本功能 | 空座、选中、已售状态清晰区分 | **场景化实现** | 中性空座、影院红选中、深灰删除线已售，并以纹理/轮廓增强非颜色识别；设计依据见 6.2 |
| 技术要求 | 不使用第三方 Chart 库 | **完整实现** | 座位图与热度图均由 `CanvasSeatMapView.js` 手写绘制，项目依赖为空 |
| 模块 1 | 按人数、年龄和观影类型智能推荐 | **完整实现** | 票数 1–20；单人、情侣、朋友、家庭、团体模式；儿童避开前三排、长者避开后三排 |
| 模块 1 | 个人/团体成员姓名和年龄输入 | **场景化实现** | 以购票必需的儿童票/银发票表达年龄条件，不额外保存同行者姓名和精确年龄，遵循最少必要原则 |
| 模块 1 | 情侣、家庭与团体连座规则 | **场景化实现** | 情侣/家庭优先同排和目标区域；团体优先同排，无解时仅降级为前后对齐的相邻两排并明确告知 |
| 模块 1 | 推荐理由 | **完整实现** | 显示座位标签、年龄建议、同行布局、偏好满足情况、跨过道情况和附加费 |
| 模块 2 | 手动单选、多选及推荐后调整 | **完整实现** | 单击为累加式 toggle，可直接连续多选；推荐只做高亮预览，用户可随时修改 |
| 模块 2 | Ctrl + 单击多选 | **场景化实现** | 普通单击已经具有追加/取消语义，同时兼容无修饰键的触摸设备；详见第 7 节 |
| 模块 2 | 拖拽选座加分项 | **额外实现** | PC 鼠标框选；移动端沿路径滑动连选，并自动排除不可用座位 |
| 模块 3 | Canvas 影院热度地图 | **完整实现** | 独立热度 Canvas 以高斯插值生成连续平面热力场，叠加于座位图下层 |
| 模块 3 | 红/黄/蓝热度与一周动态变化 | **完整实现** | 冷蓝—暖黄—橙红渐变，支持周一至周日和一周综合 8 种即时切换 |
| 模块 4 | 视角、银幕距离、周围空位体验评分 | **完整实现** | 三项指定指标全部纳入，并增加价格平衡维度；输出总分、分档和文字解释 |
| 模块 4 | 观影后人工评分与系统评分综合 | **场景化实现** | 本地单机数据无法形成可信群体评价，因此以实时四维系统评分替代伪公共评分；边界与后续方案见第 7、11 节 |
| 模块 5 | 大字体、高对比、色盲友好 | **完整实现** | “辅助功能”中三项独立开关，可按访客或账户持久化，并同步重绘 Canvas |
| 模块 5 | 可选语音提示 | **场景化实现** | 使用标准 `aria-live` 与完整语义座位层交由屏幕阅读器播报，避免公共场景主动发声或重复朗读 |
| 模块 6 | 预订、取消预订、购票、退票 | **完整实现** | 10 分钟锁座、主动释放/超时、确认订单、订单查询、政策驱动整单退票和库存恢复 |
| 模块 6 | LocalStorage 或本地 JSON，无后端数据库 | **完整实现** | `smartcinema_state` 为唯一事实源，含版本校验、引用校验、乐观并发和备份恢复 |
| 交互设计 | 用户流程不多于 3 步 | **完整实现** | “选择场次 → 票数与座位 → 确认订单”三阶段进度条与移动端固定摘要 |
| 视觉设计 | 科技感、美学、色彩/布局/字体/风格说明 | **完整实现** | 暖色影院主题、深色座位舞台、液态玻璃容器；完整说明见第 6 节 |
| 响应式 | 支持 PC、iPad、手机 | **完整实现** | 响应式布局、粗/细指针能力查询、触摸滑选、平移、双指缩放和键盘导航 |
| AI 协同 | 产品、用户、AI 生成、人工修改和 UX 优化记录 | **完整实现** | 第 2、8 节及 `docs/` 技术材料记录完整过程与设计判断 |
| 加分项 | AI 观影问答式顾问 | **额外实现** | 自然语言转六维目标向量、最多 5 组合法候选、理由解释、本地规则回退 |
| 加分项 | 动画交互与个性化界面 | **额外实现** | 物理回弹、手势状态机、热度淡入、液态玻璃和减少动态降级 |

综上，基础功能和六个核心模块均有可验收实现；与题目示例形态不同的部分均给出了对应能力、实际入口和真实场景设计依据。

## 4. 技术架构

### 4.1 技术选型

| 项目 | 选择 | 原因 |
|---|---|---|
| 页面技术 | HTML5 + CSS3 + 原生 JavaScript | 满足课程要求，保持运行依赖最小 |
| 模块系统 | ES Modules | 保留明确模块边界，无需打包器 |
| 图形绘制 | Canvas 2D API | 自主实现弧形布局、命中检测和热力场，不使用 Chart 库 |
| 持久化 | LocalStorage + sessionStorage | 构建无后端数据库的本地交易演示 |
| 本地服务 | Node.js 静态服务器 | 提供模块加载和 AI 接口代理，密钥不进入浏览器 |
| 测试 | 自研 Node 测试运行器 + 浏览器契约页 | 覆盖领域、应用、存储、交互和架构边界 |

`package.json` 的运行时依赖和开发依赖均为空。项目没有构建步骤，Canvas、推荐与状态管理均为团队自行实现。

### 4.2 分层结构

```text
UI 层 → Application 应用层 → Domain / Shared 领域与共享层
                      ↑
             Infrastructure 基础设施层
```

```text
src/
├── domain/                 领域实体与业务规则
│   ├── catalog/            影片、影院、影厅、场次、票价与退票政策
│   ├── booking/            草稿、库存、锁座、选座规则、体验与偏好向量
│   ├── order/              不可变订单快照与取消逻辑
│   └── user/               用户、角色与辅助偏好
├── application/ticketing/  购票、账户、推荐、偏好和运维用例
├── infrastructure/         演示目录、本地存储、浏览器时钟、AI 解释器
├── ui/                     Canvas 视图、控制器、弹窗和视图工具
├── ticketing.js            消费者页面编排
└── internal.js             运维页面编排
```

领域层和应用层不直接访问 DOM、LocalStorage、系统时间或随机源。这些能力通过 `bootstrapTicketing.js` 和 `bootstrapInternal.js` 注入，使核心规则能够独立测试，也避免 UI 自行计算价格或直接修改库存。

### 4.3 单一状态源

生产事实源为 LocalStorage 中的 `smartcinema_state`，当前状态格式版本为 4：

| 字段 | 含义 |
|---|---|
| `usersById` / `session` | 用户与当前登录会话 |
| `ordersById` | 已确认或已取消的订单快照 |
| `inventoriesByShowtime` | 每个场次的已售座位和锁座映射 |
| `holdsById` | 锁座状态机记录 |
| `settingsByUser` | 访客或账户的辅助偏好 |
| `schemaVersion` / `revision` / `updatedAt` | 格式版本、乐观并发版本与更新时间 |

存储层在每次读取时重建领域对象并检查引用完整性，例如库存中的锁座必须真实存在、已确认订单的座位必须已售、已消费锁座必须关联对应订单。写入采用 `revision` 比较避免旧页面覆盖新状态，写入后立即回读比对。低版本开发数据会清理并重新初始化；损坏或高版本数据不会被静默覆盖。

sessionStorage 只保存当前选座草稿和访客会话标识；管理员导入完整备份前会保存可回滚快照。

## 5. 核心功能设计与实现

### 5.1 目录、影厅与场次

演示目录完全本地生成，运行时不依赖影片或图片服务。影片海报已保存到 `public/images/posters/`，加载失败时保留 CSS 抽象封面。

三种影厅规模与题目要求一致，但采用真实影院常见的“编号 + 制式”命名：

| 规模 | 布局 | 实际影厅名 |
|---|---|---|
| 小厅 | 10 排 × 10 座 = 100 座 | 2 号厅、3 号厅 |
| 中厅 | 10 排 × 20 座 = 200 座 | 5 号激光厅、6 号激光厅 |
| 大厅 | 10 排 × 30 座 = 300 座 | IMAX 厅、3 号巨幕厅 |

行号使用 A–K 并跳过 I，避免与数字 1 混淆。每个影厅的 H 排两侧设置轮椅位与陪同席，并标记为无台阶区域。库存用 `场次 ID + 座位 ID` 的稳定哈希生成，初始已售比例约为 22%，刷新后保持一致。

### 5.2 票种、定价与退票政策

系统提供成人票、儿童票、学生票和银发票。所有金额用“分”的非负整数保存。报价由以下部分组成：

```text
订单总价 = 基础票价 + 票种调整 + 座位区附加费 + 每张 ¥3 服务费
```

基础票价根据早场、黄金时段和晚场政策变化；优选区附加费为 ¥8 或 ¥12。UI 不自行拼接价格，所有显示都来自领域报价 `PricingQuote`，保证座位变化、票种变化和结算金额一致。

退票政策在下单时写入订单快照：开场前 60 分钟允许整单退票，每单收取 ¥5 手续费。退票操作同时更新订单和释放整组已售座位，重复取消保持幂等。项目没有模拟部分退票，因为真实售后需要重新核算票种、连座与手续费，整单退能够保持本地演示的一致性。

### 5.3 智能连座推荐

推荐算法遵循“生成空间候选 → 校验硬规则 → 排序 → 输出解释”的顺序。

1. 按排收集可用座位，使用滑动窗口枚举同排连续座位；
2. 对 3 人以上的朋友、家庭和团体，额外生成同分区、前后对齐的相邻两排紧凑方案；
3. 通过统一的 `validateSeatSelection` 检查票座数量、库存、分区、无障碍席、陪同席和新制造孤座；
4. 先比较儿童/长者建议，再比较同排程度、跨过道、用户偏好、观影体验、附加费等；
5. 做排次多样化，最多返回 5 组并生成推荐理由。

儿童票默认避开前三排，银发票默认避开后三排。只要存在完全符合年龄建议的候选，算法就不会使用违规候选；只有全场不存在合规紧凑布局时才降级，并在理由中明确告知。手动选座保留用户最终决定权，因此年龄规则作为提示而非禁止；“需要无台阶通行”则始终作为硬条件。

如果当前场次没有完整连座，系统会继续搜索同一影片、影院和营业日中时间最近的可售场次，避免只返回“无座”。

### 5.4 AI 自然语言座位顾问

AI 顾问不直接生成座位号，而是把“想坐近一点”“预算有限但不要太偏”“带老人、希望进出方便”等自然语言转成六维目标向量：银幕距离、居中程度、周边空位、价格水平、少受打扰和进出便捷。

远端解释器要求 JSON 结构化输出，使用低温度和 8 秒超时；输出进入业务层前还要进行范围截断、标签白名单过滤和权重归一化。API 不可用、未配置密钥或请求失败时，系统自动使用本地关键词规则生成相同结构的意图，因此核心选座流程不依赖外部服务。

合法候选的客观向量为 $x$，用户目标为 $t$，归一化权重为 $w$，匹配距离为：

$$
d(x,t)=\sqrt{\sum_i w_i\left(\frac{x_i-t_i}{100}\right)^2}
$$

AI 得分只用于已经通过业务校验的候选之间排序，不能绕过库存、连座、无障碍或孤座规则。推荐结果先以高亮预览呈现，只有用户点击“采用”才写入本单；若库存版本发生变化，预览会失效并要求重新生成。

### 5.5 手动选座与跨端手势

- 桌面端单击座位会累加式切换选择，不需要按住修饰键；
- 鼠标或手写笔可拖出矩形批量框选普通座位；
- 移动端从座位起手可沿路径连续选择或取消；
- 从空白处起手用于平移，两指用于缩放；
- 键盘 Tab 只进入座位图一次，方向键移动虚拟焦点，Space 选择；
- 推荐只是高亮候选，用户随时可以改为手动选择。

移动端交互采用显式手势状态机，统一把触摸坐标换算到缩放和平移后的 Canvas 坐标，避免浏览器滚动、合成 click、连续选座和平移争夺同一手势。

### 5.6 热度地图与体验评分

座位层和热度层使用两个 Canvas 分开绘制。热度层不是离散色块，而是以座位中心为核、在低分辨率位图上进行高斯插值后放大，形成连续区域。色带采用冷蓝—暖黄—橙红，一周七日和综合模式通过稳定日因子与确定性扰动展示变化。

系统评分包含四个维度：

| 维度 | 主要依据 | 权重 |
|---|---|---:|
| 视野角度 | 座位块相对水平中心的偏移 | 35% |
| 银幕距离 | 相对约 0.58 倍纵向甜点位置的偏移 | 30% |
| 周边空位 | 同排同区附近座位的不可用比例 | 20% |
| 价格平衡 | 平均座位附加费相对最高附加费 | 15% |

最终输出“极佳、优秀、舒适、基础”四档，并自动指出最强和相对较弱维度。增加价格维度是为了避免系统只把用户推向最贵区域，使结果更接近真实购票中的性价比判断。

### 5.7 锁座、订单与并发一致性

锁座使用以下状态机：

```text
pending → held → consumed（确认订单）
             ├→ expired（10 分钟到期）
             └→ released（主动关闭或运维释放）
```

锁座和库存映射成组更新，任何一个座位冲突都会使整组预订失败，不产生部分占座。重复锁座或重复确认通过幂等键返回原结果。浏览器刷新后，同一会话可恢复仍有效的锁座；发生抢座冲突时，系统移除不可用选择并引导用户继续，而不是让流程回到起点。

订单从已消费锁座创建，保存影片、影院、影厅、场次、座位、报价和退票政策快照。目录以后发生变化也不会改变历史订单语义。

### 5.8 管理员运维入口

管理员后台提供场次容量、已售数、锁座数、可用数、上座率、订单、退款和用户摘要；支持清理过期锁座、人工释放锁座、导出脱敏诊断数据、导出完整备份及导入恢复。普通会员和访客均无法读取运维数据。

后台没有提供影片、场次和票价的在线编辑。演示目录是本地确定性数据，运维功能重点放在交易状态和库存一致性，而不是构建另一套内容管理系统。

## 6. 交互、视觉与无障碍设计

### 6.1 视觉系统

购票页采用暖色影院主题与液态玻璃风格：浅暖页面底色承载内容，座位区使用深色舞台形成沉浸对比，影院红作为主要操作强调色，优选区使用金色，无障碍席使用蓝色。

| 用途 | 主要色值 |
|---|---|
| 页面底色 | `#f7f5f2` |
| 面板 | `#ffffff` |
| 主文字 / 次文字 | `#181817` / `#6f6c66` |
| 主要强调 | `#d43f45` |
| 座位舞台 | `#101319` |
| 优选区 / 无障碍席 | `#e0b35b` / `#4e93c6` |

液态玻璃容器由背景渐变、半透明材质、滤镜、渐变边缘、内外阴影和内容层组合而成。若浏览器不支持 `backdrop-filter`，或用户要求高对比/减少透明度，容器会回退为不透明实色，不影响核心交互。

字体使用系统无衬线栈：Inter、系统 UI 字体、苹方、微软雅黑依次回退，不从网络加载字体。影片原名使用 Georgia 衬线形成海报感。响应式布局结合宽度断点、粗指针与悬停能力查询，而非只按屏幕宽度判断。

### 6.2 状态配色的场景化选择

作业示例使用绿色空座、黄色选中、红色已售。我们经过对真实选座界面和深色座位舞台的分析，采用了“按可操作性分配视觉权重”的方案：空座保持中性，已售座位降为深灰并增加删除线，用户当前选择使用唯一的高饱和影院红。

这样做有三点考虑：

1. 用户最需要快速确认的是“自己选了什么”，而不是让大量已售座位占据最高视觉权重；
2. 红绿并置对常见红绿色觉障碍并不友好，状态还应依靠明度、纹理和轮廓区分；
3. 全站的红色统一表示当前操作、主按钮和已选状态，语义更一致。

色觉友好模式进一步为已售席增加斜纹、为已选席增加内嵌白环、为优选区增加不同轮廓，即使去除色相仍可区分。

### 6.3 无障碍能力

系统提供四项独立且可持久化的偏好：大字体与宽松间距、高对比度、色觉友好和减少动态。Canvas 旁同步维护隐藏语义按钮层，每个座位可读出排号、座号、附加费、类型和状态。

此外还实现了跳转链接、全局可见焦点、弹窗焦点陷阱与焦点归还、`aria-live` 状态播报、键盘选座、`prefers-reduced-motion` 和 `prefers-contrast` 媒体查询。我们选择标准读屏播报而不是主动 Web Speech 朗读，因为前者由用户已有辅助技术控制，不会与屏幕阅读器重复发声，也更适合公共影院环境。

## 7. 面向真实场景的设计决策

本节说明项目中与作业示例形态不同的部分。这些不是对要求的回避，而是团队在保留能力目标的前提下，对真实业务、隐私、设备差异和库存约束作出的产品判断。

| 题目示例 | 项目方案 | 场景化考虑 |
|---|---|---|
| 输入每位观众姓名和年龄 | 用票种表达儿童/长者条件，不采集同行成员信息 | 精确姓名和年龄并非推荐必需；减少代他人提交个人信息 |
| 团体必须同排 | 同排优先，无解时仅降级为相邻两排紧凑布局 | 高上座率下硬性同排容易直接无解；相邻两排比拒绝服务更可用 |
| Ctrl + 单击多选 | 普通单击本身就是累加 toggle，并提供框选和触摸滑选 | 触摸设备没有 Ctrl；macOS Ctrl + 单击还有系统右键语义 |
| 登录后方可操作 | 访客可浏览、试选和限时锁座，确认订单前必须登录 | 推迟身份门槛，避免对未购买用户过度采集信息 |
| 影厅称为小/中/大厅 | 使用编号和 IMAX/激光/巨幕等制式名称，同时显示准确座位数 | 更符合真实影院对外命名方式，也不会把“小厅”暗示为低品质 |
| 每个座位重复显示排号和座号 | 座位内显示座号，排号置于每排两侧；完整标签在提示、结算和读屏层提供 | 25×20 像素内重复排号会显著降低可读性，手机缩放后更明显 |
| 三档体验标签 | 四档“极佳/优秀/舒适/基础” | 把较大的低分区间拆开，减少“一般”对可接受座位的负面暗示 |
| AI 问答式顾问 | 单轮自然语言输入、结果预览、可重新描述或手动调整 | 座位偏好参数有限，单轮更符合三步购票目标；减少第三方数据累积 |
| 注册即享会员资格 | 注册用户获得 `member` 角色与购票/订单能力，不虚构折扣权益 | 没有真实商业主体时不承诺无法兑现的折扣、积分与有效期 |
| 单文件可双击运行 | ES Modules 分层源码，通过 `npm start` 运行 | 课程允许其他形态并要求说明；分层、测试和依赖注入比压入单文件更可维护 |

关于观影后手动评分，我们判断纯 LocalStorage 环境无法汇总不同观众的真实反馈，单机样本也不足以形成可信的群体结论，因此本版本聚焦于购票前的系统评分与解释，没有把本机手工输入伪装成平台公共评分。WebSocket 多人在线同步属于额外加分能力，与当前无后端数据库、单机演示的边界不同，本版本通过 `revision` 乐观锁、库存冲突检测和恢复路径展示并发一致性思想。

## 8. AI 协同开发说明

### 8.1 协作方式

团队采用“需求拆解—AI 生成初版—人工审查—浏览器验证—自动化回归—再次迭代”的协作方式。AI 编程助手主要用于：

- 将课程要求拆解为领域对象、用例和验收条件；
- 生成 Canvas 布局、推荐算法、状态机和测试的初始实现；
- 辅助排查坐标变换、移动端手势和状态一致性问题；
- 生成文档初稿、测试思路和重构建议。

团队成员负责最终产品判断、规则优先级、隐私边界、视觉取舍、代码审查、实际调试与合入决定。AI 输出不能直接作为完成标准，必须经过代码阅读和测试。

### 8.2 主要人工修改与迭代

1. **从通用“好坏分”改为目标向量。** 初版 AI 推荐无法区分“过近”和“过远”两种低分；团队将六个维度改成客观程度，用目标向量和加权欧氏距离表达相反方向的偏好。
2. **将规则校验移到推荐评分之前。** 避免高分掩盖已售、非连座、无障碍或孤座违规。
3. **重构移动端手势所有权。** 把连续选座、平移和双指缩放拆成显式状态，统一坐标变换并阻止触摸结束后的合成点击。
4. **加入预览—采用边界。** AI 返回结果只高亮，不直接覆盖用户选择；库存变化时预览失效。
5. **统一交易状态。** 将早期分散的订单与库存数据整合为带版本、锁座和不可变订单快照的单一状态源。
6. **持续视觉与无障碍优化。** 双 Canvas 分层、液态玻璃实色回退、键盘虚拟焦点、读屏语义层和多项账户偏好均来自多轮人工验收与调整。

### 8.3 团队协作

项目使用 Git 与 GitHub 进行版本管理。成员在功能分支上开发，通过提交历史和阶段性集成完成需求迭代；合并前结合自动化测试、浏览器检查和代码评审。答辩材料、技术解读和报告与最终源码统一保存在 `experiment/final-sprint` 分支。

## 9. 测试与质量保障

### 9.1 Node 自动化测试

在 `experiment/final-sprint@3e06261` 上执行：

```bash
npm test
```

结果为 **84/84 通过，0 失败**：

| 测试组 | 结果 | 主要覆盖 |
|---|---:|---|
| Ticketing Domain | 21/21 | Money、目录、Canvas 布局、选座规则、锁座、订单与退票 |
| State Storage | 9/9 | 初始化、版本、损坏数据、引用完整性、乐观并发 |
| Booking Service | 21/21 | 推荐、报价、替代场次、锁座幂等、抢座、退票 |
| Ticketing Composition | 9/9 | 组合根、账户、偏好、草稿和跨刷新恢复 |
| Operations Service | 9/9 | 运维权限、指标、锁座释放、备份和恢复 |
| Architecture Boundaries | 6/6 | 入口、依赖方向、Canvas 与浏览器边界 |
| Seat Map Controller | 9/9 | 触摸、缩放锚点、阻尼、回弹和状态提示 |

### 9.2 浏览器验收

仓库提供 `tests/browser-regressions.html`，用于在独立的 `http://127.0.0.1:8080` origin 下验证消费者流程、响应式布局、弹窗、键盘操作、运维权限与运行时错误，并避免污染日常 `localhost` 数据。

当前测试脚本定义 21 项契约目标；测试页说明文字仍写“19 个 PASS”，与脚本中的 21 项目标不同，提交前应以最新稳定版 Chrome/Edge/Safari 实际运行结果为准并同步该文案。Node 契约已全部通过；PC、iPad 和手机仍应进行最终人工视觉验收，特别关注 320px 目录宽度、移动端座位缩放和目录切换后的焦点归还。

## 10. 运行与用户手册

### 10.1 环境与启动

要求 Node.js 18 或更高版本。项目使用模块脚本，不能通过 `file://` 双击运行。

```bash
npm install
npm start
```

消费者入口：`http://localhost:8080/`  
运维入口：`http://localhost:8080/internal.html`

演示管理员账号：`admin`  
演示管理员密码：`admin123`

这是本地演示凭据，密码以明文存储在当前浏览器，请勿录入真实密码或个人信息。若使用远端 AI 座位偏好解释，应按 `.env.example` 配置服务端环境变量；未配置时会自动使用本地规则解释器。

### 10.2 消费者操作

1. 选择影片、影院、日期和可售场次；
2. 调整票种和人数，选择一键推荐、智能顾问或手动选座；
3. 点击继续创建 10 分钟锁座，登录/注册后确认订单。

“我的订单”中可查看取票码和退票资格。符合政策时可取消整单并释放座位。

### 10.3 管理员操作

1. 在消费者入口使用管理员账号登录；
2. 打开 `internal.html`；
3. 查看经营指标、场次库存和锁座；按需清理过期锁座、人工释放或导出诊断/备份。

完整备份包含本地演示凭据，导出文件不应公开分享；脱敏诊断快照不可用于恢复。

## 11. 项目边界与后续方向

SmartCinema 是本地课程演示，不是生产认证、支付或验票系统。当前边界包括：

- 不接入真实支付渠道、退款回调和二维码验票；
- 退款申请状态不会得到真实支付平台的最终回调；
- 影片以真实条目作演示，影院名称、地址、排期、价格、库存和星期热度均为虚构数据；
- 本地账户密码为明文演示凭据，LocalStorage 权限不能替代服务端鉴权；
- 不汇总跨设备、跨用户的观影后评分；
- 真实读屏、不同移动浏览器和极端窄屏仍需要目标设备人工验收。

若继续产品化，优先方向是：接入服务端账户与支付、使用 WebSocket/推送实现跨设备库存同步、补充实名或特殊场次的合规信息采集、引入真实匿名观影反馈、完善锁座和退款监控，并对 Chrome、Safari 与主流读屏软件进行设备矩阵测试。

## 12. 参考资料与学术诚信说明

1. 清华大学软件学院，《Web 前端技术实训》2026 夏大作业说明：SmartCinema 智能影院选座系统。
2. MDN Web Docs, [Canvas API](https://developer.mozilla.org/zh-CN/docs/Web/API/Canvas_API)。
3. MDN Web Docs, [Window.localStorage](https://developer.mozilla.org/zh-CN/docs/Web/API/Window/localStorage)。
4. W3C, [WAI-ARIA Authoring Practices Guide](https://www.w3.org/WAI/ARIA/apg/)。
5. W3C, [Web Content Accessibility Guidelines (WCAG) 2.1](https://www.w3.org/TR/WCAG21/)。
6. TMDB（The Movie Database）：演示影片封面来源；页面内已标注来源，图片保存为本地资源。
7. Apple、Netflix、猫眼电影、淘票票等产品的内容层级、选座和结算流程，用作交互与视觉参考。
8. 项目内液态玻璃视觉原型：`ref/liquid_glass_demo.html`、`ref/liquid_glass_stack_demo.html`。
9. AI 辅助工具：Claude Code、GitHub Copilot、Codex/ChatGPT、DeepSeek。其参与范围与人工修改见第 8 节。

本项目代码、设计决策和文档由小组成员共同完成。AI 生成内容均经过成员审查、修改和测试；引用的技术资料、素材来源与大模型使用在本报告中列明。我们对最终提交内容及其准确性负责。

---

## 附录 A：要求与实现对应表

| 要求模块 | 对应实现 | 说明 |
|---|---|---|
| 登录注册与管理员 | `src/application/auth/`、`internal.html` | 注册获得 member 角色；管理员入口独立授权 |
| 100/200/300 座弧形影厅 | `DemoCatalogRepository.js`、`CanvasSeatMapView.js` | 10 排，原生 Canvas，无第三方 Chart 库 |
| 智能推荐选座 | `RecommendSeatBlock.js` | 同行、票种、年龄建议、偏好、连座与替代场次 |
| 手动选座 | `SeatMapController.js` | 单击多选、鼠标框选、触摸滑选、键盘选座 |
| 影院热度地图 | `SeatDecisionGuide.js`、`CanvasSeatMapView.js` | 双 Canvas、连续热力场、一周动态切换 |
| 观影体验评分 | `SeatDecisionGuide.js` | 四维系统评分与解释；未模拟无群体数据支撑的本机公共评分 |
| 无障碍模式 | `Settings.js`、`accessibility.css`、语义座位层 | 四项偏好、焦点管理、读屏播报和无障碍席约束 |
| 订单中心 | `SeatHold.js`、`TicketOrder.js`、`BookingService.js` | 锁座、确认、订单查询、整单退票、幂等与库存恢复 |
| 响应式布局 | `ticketing.css`、`SeatMapController.js` | PC、平板、手机布局与触摸手势 |
| AI 顾问 | `SeatAdvisorController.js`、`infrastructure/ai/` | 自然语言意图、结构化约束、本地回退、预览后采用 |
| 拖拽与动画优化 | `SeatMapController.js`、`DialogController.js` | 框选、滑选、缩放、物理回弹、减少动态 |
| 个性化界面 | `ticketing.css`、`ref/` | 暖色影院主题与液态玻璃容器 |
| 说明文档 | 本报告、`README.md`、`docs/`、`thubeamer/` | 产品、用户、技术、AI、用户手册、设计与参考资料 |
